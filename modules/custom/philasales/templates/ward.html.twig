{#
/**
 * @file
 * Default theme implementation to present a ward entity.
 *
 * This template is used when viewing a registered ward's page,
 * e.g., /admin/content/ward)/123. 123 being the ward's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_ward()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
